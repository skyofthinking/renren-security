<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.renren.modules.sys.dao.SysConfigDao">
    <select id="queryObject" resultType="io.renren.modules.sys.entity.SysConfigEntity">
		select * from sys_config where uid = #{value}
	</select>

    <select id="queryList" parameterType="map" resultType="io.renren.modules.sys.entity.SysConfigEntity">
        select * from sys_config where status = 1
        <if test="keyword != null and keyword.trim() != ''">
            and keyword like concat('%', #{keyword} ,'%')
        </if>
    </select>

    <select id="queryTotal" parameterType="map" resultType="int">
        select count(*) from sys_config where status = 1
        <if test="keyword != null and keyword.trim() != ''">
            and keyword like concat('%',#{keyword},'%')
        </if>
    </select>

    <insert id="insert" parameterType="io.renren.modules.sys.entity.SysConfigEntity">
		insert into sys_config(uid, keyword, value, remarks)
			values(#{uid}, #{keyword}, #{value}, #{remarks})
	</insert>

    <update id="update" parameterType="io.renren.modules.sys.entity.SysConfigEntity">
		update sys_config set keyword = #{keyword}, value = #{value}, remarks = #{remarks}
			 where uid = #{uid}
	</update>

    <!-- 根据key，更新value -->
    <update id="updateValueByKey" parameterType="map">
		update sys_config set value = #{value} where keyword = #{keyword}
	</update>

    <!-- 根据key，查询value -->
    <select id="queryByKey" parameterType="string" resultType="io.renren.modules.sys.entity.SysConfigEntity">
		select * from sys_config where keyword = #{keyword}
	</select>

    <delete id="deleteBatch" parameterType="int">
        delete from sys_config where uid in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>